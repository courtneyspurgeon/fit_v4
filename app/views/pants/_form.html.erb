<%= form_for(@pant) do |f| %>
  <% if @pant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pant.errors.count, "error") %> prohibited this pant from being saved:</h2>

      <ul>
      <% @pant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :brand_id %><br />
    <%= f.collection_select :brand_id, Brand.all, :id, :name,  {:include_blank => 'None'} %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.select :year, options_for_select((Time.now.year.to_i).downto(1995).to_a)%>
  </div>
  <div class="field">
    <%= f.label :style %><br>
    <%= f.number_field :style %>
  </div>
  <div class="field">
    <%= f.label :price_range %><br>
    <%= f.number_field :price_range %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
